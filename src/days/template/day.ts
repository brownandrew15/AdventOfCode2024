import { Day } from '../day'

export default class Day0 extends Day {
  constructor () {
    super("0");
  }

  partOne (input: string): string {
    return "";
  }

  partTwo (input: string): string {
    return "";
  }
}